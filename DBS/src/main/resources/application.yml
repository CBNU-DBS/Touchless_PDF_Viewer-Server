spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/DBS
    username: root
    password: 12340119
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql : true
        format_sql: true
        use_sql_comments: true
    database: MYSQL

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace
